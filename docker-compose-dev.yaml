services:
  proxy:
    extends:
      file: docker-compose.yaml
      service: proxy
  inference-0:
    extends:
      file: docker-compose.yaml
      service: inference-0
    # these lines are needed for CUDA acceleration
    # devices:
    #   - nvidia.com/gpu=all
  server:
    extends:
      file: docker-compose.yaml
      service: server
    image: logdetective/server
  migrate:
    extends:
      file: docker-compose.yaml
      service: migrate
    image: logdetective/migrate
  worker:
    extends:
      file: docker-compose.yaml
      service: worker
    image: logdetective/server
  maintenance-worker:
    extends:
      file: docker-compose.yaml
      service: maintenance-worker
    image: logdetective/server
  postgres:
    extends:
      file: docker-compose.yaml
      service: postgres
  user-contributions-update:
    extends:
      file: docker-compose.yaml
      service: user-contributions-update
    profiles:
      - sync

volumes:
  database_data:
    driver: local
